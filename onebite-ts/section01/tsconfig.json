{
  "compilerOptions": {
    "skipLibCheck": true,
    "target": "ESNext",
    "module": "ESNext",
    "outDir": "dist",
    "strict": true,
    "moduleDetection": "force"
  },
  "include": ["src"]
}

// target : 컴파일해서 만들어지는 JS 코드의 버전을 설정하는 옵션

// include 옵션을 이용하면 특정 폴더에 있는  모든 ts 파일을 동시에 한 번의 명령으로 컴파일하도록 설정할 수 있음 (ESNext는 최신버전)

// module : JS 코드의 모듈 시스템을 설정하는 모듈 옵션

// outDir : 컴파일 결과 생성되는 JS 파일이 어디 위치했으면 좋겠는지 작성해주면 된다.

// strict : TS가 타입 검사를 할 때 얼마나 엄격하게 할 지

// moduleDetection : TS가 각 파일을 어떤 모듈로 감지할 것이냐를 결정하는 옵션

// skipLibCheck :  불 필요한 타입 정의 파일의 타입 검사를 생략하도록 설정
