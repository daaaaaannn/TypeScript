// export const hello = (message) => {
//   console.log('Hello' + message);
// };
//  es모듈

// ts 에서는 매개변수의 타입을 개발자들이 지정하도록 권장함
// 따라서 추론이 되지 않는 변수의 타입을 명시하지 않으면 strict 모드를 true로 설정했을 때 오류 발생한다.

const a = 1;
